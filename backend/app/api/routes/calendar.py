"""Earnings calendar routes — implemented in Phase 4."""

from fastapi import APIRouter

router = APIRouter(prefix="/calendar", tags=["calendar"])

# Endpoints will be added in Phase 4:
# GET /
# GET /{ticker}
